# 安全政策

## 支持范围

安全修复优先覆盖 `main` 分支和最新 GitHub Release。早期 Alpha 版本可能不再单独维护。

## 私密报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要为尚未修复的漏洞创建公开 Issue，也不要附加真实专利材料、API Key、访问令牌、模型请求全文或个人信息。

报告中可以包含：受影响版本、最小化复现步骤、预期影响、建议修复方式，以及不含敏感数据的合成样例。维护者会尽力在 7 天内确认收到报告，但该时间不构成服务等级承诺。

## 安全边界

- 本地 Web 服务仅设计为绑定 `127.0.0.1`，不应直接暴露到公网。
- 项目没有内置多用户鉴权、租户隔离或公网部署防护。
- 外部模型模式会把完成请求所需的数据发送给用户配置的服务商。
- 生成的专利文本不是法律意见，不能替代专业审查。
- 对非本仓库拥有或未获授权的系统进行扫描不属于本政策授权范围。

## 密钥泄露

如果密钥曾被提交、上传到 Issue 或包含在发布包中，请立即在对应服务商处撤销并轮换；仅删除文件或提交并不能使已泄露密钥重新安全。
